name: firebase_upload
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # upload file to firebase
  firebase_core: ^1.0.3
  firebase_storage: ^10.2.17

  # pick file
  file_picker: ^4.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flex_color_picker: ^2.4.0
flutter_launcher_icons: ^0.8.1

flutter:
  uses-material-design: true

  